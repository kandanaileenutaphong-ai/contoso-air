apiVersion: v1
kind: Service
metadata:
  name: contoso-air-kandanai
  namespace: dev
  labels:
    app.kubernetes.io/name: contoso-air-kandanai
    kubernetes.azure.com/generator: devhub
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: contoso-air-kandanai
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000